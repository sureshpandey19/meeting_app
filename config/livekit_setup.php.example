<?php

return [
    // Generate a token, then store its hash here.
    // Example: php -r 'echo password_hash("your_setup_token", PASSWORD_DEFAULT), PHP_EOL;'
    'token_hash' => 'change_this_hash',
    // Mark true after first successful LiveKit config save.
    'used' => false
];
